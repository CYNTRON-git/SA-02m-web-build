# Tests for sa02m-modbus-mqtt (unittest discover / pytest). Dev-side only —
# never executed on the device; no runtime deps beyond the stdlib (third-party
# bridge imports are stubbed when absent, see test modules).
