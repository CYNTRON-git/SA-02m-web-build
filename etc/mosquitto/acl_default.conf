# SA-02m — Mosquitto ACL (шаблон)
# Устанавливается в /etc/mosquitto/acl/default.conf

# Пользователь mqttuser — доступ с ПК (порт 1884).
# Топики моста: /devices/… (Wiren Board) — обязателен ведущий «/».
user mqttuser
topic readwrite /devices/#
topic read $SYS/#
