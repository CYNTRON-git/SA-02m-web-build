# SA-02m network tuning
# Send gratuitous ARP when interface link comes up.
# This notifies all LAN hosts of our MAC immediately,
# preventing stale/missing ARP entries after reboot.
net.ipv4.conf.eth0.arp_notify = 1
