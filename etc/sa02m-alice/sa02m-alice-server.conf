[gateway]
# Cyntron Alice Gateway (Phase 0). Controllers never store Yandex OAuth tokens.
wss_url = wss://alice.cyntron.ru/controller/socket.io
http_url = https://alice.cyntron.ru
sio_path = /socket.io
